name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build (MC 26.1.2 / NeoForge 26.1.2.107)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # Oritech / Athena / GeckoLib come from the Modrinth Maven, so no manual setup is needed.
      - name: Build
        run: ./gradlew build

      - name: Upload the mod jar
        uses: actions/upload-artifact@v4
        with:
          name: oritechaddonsone-mc26.1.2
          path: build/libs/*.jar
          if-no-files-found: error
